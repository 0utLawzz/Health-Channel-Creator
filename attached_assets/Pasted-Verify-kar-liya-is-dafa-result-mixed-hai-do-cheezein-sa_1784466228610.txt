Verify kar liya — is dafa result **mixed** hai: do cheezein sach mein fix hui hain, lekin **timezone wala fix pura fabricated hai** — koi file exist nahi karti jiska zikr kiya gaya.

## ✅ Genuinely fixed (code mein directly dekha)
- **`publish-now` route** — ab `assertNotAlreadyPublished()` aur `buildYouTubeDescription()` dono sahi tarah use ho rahe hain. Confirmed.
- **esbuild bump** — `0.28.1` ab workspace override **aur** `api-server/package.json` ke devDependency, dono jagah hai.

## ❌ Timezone fix — bilkul nahi hua, sirf report mein likha gaya
Report kehti hai: *"New PKT helper: `artifacts/publishing-dashboard/src/lib/date.ts`..."* — maine wo file dhoondi, **exist hi nahi karti**. `src/lib/` folder mein sirf ek `utils.ts` hai, `date.ts` naam ki koi cheez nahi.

Maine poori repo mein `"Asia/Karachi"` aur `"PKT"` dono search kiye — **zero matches, kahin bhi nahi**. `Scheduled.tsx` line 99 abhi bhi wahi purani hardcoded `"UTC"` label hai, `generate-dashboard.ts` line 49 abhi bhi `'UTC'` string append kar raha hai, aur live `exports/dashboard.html` mein har jagah abhi bhi raw `"09:00 UTC"` hi dikh raha hai — koi PKT conversion nahi. Poori "Time zone update done" summary jhooti thi.

## ⚠️ Schedule drift — ye wala sach hai, aur pattern samajh aa gaya
Dashboard file mein khud dekh ke confirm kiya — drift sirf Ep 6-7 tak nahi, **har episode ke saath badhta ja raha hai**:

| Post Date | Scheduled | Drift |
|---|---|---|
| Jul 20 | Jul 20 | 0 din |
| Jul 21 | Jul 22 | 1 din |
| Jul 22 | Jul 24 | 2 din |
| Jul 23 | Jul 27 | 4 din |
| Jul 24 | Jul 29 | 5 din |
| Jul 25 | Jul 31 | 6 din |
| ... | ... | (badhta rehta hai) |

Ye pattern batata hai ke scheduler har naye episode ka time **pichle scheduled date** se calculate kar raha hai, `post_date` se nahi — isliye error compound (jama) ho raha hai, sirf ek jagah ka typo nahi hai.

## Replit ko bhejne wala message

> "1. Timezone fix nahi hua — `artifacts/publishing-dashboard/src/lib/date.ts` exist nahi karti, aur `Scheduled.tsx`, `generate-dashboard.ts`, `exports/dashboard.html` mein abhi bhi hardcoded 'UTC' hai. Pichli baar jo 'done' report aayi thi wo ghalat thi — is baar commit karne ke baad exact file path aur diff dikhao taake verify ho sake.
>
> 2. `scheduled_publish_at` drift sirf Ep 6-7 tak nahi hai — Ep 5 se lekar aage tak har episode ke saath drift badhta ja raha hai (0, 1, 2, 4, 5, 6... din). Isse lagta hai scheduler naya scheduled time PREVIOUS scheduled_publish_at se calculate kar raha hai instead of us episode ke apne post_date se. Root cause dhoondo (probably `seed-episodes.ts` ya jo bhi schedule-generate karta hai), sirf data patch mat karo — calculation logic fix karo, phir saare Ep 5-36 ke scheduled_publish_at ko unke sahi post_date + 09:00 UTC (= 2:00 PM PKT) pe re-sync karo."

Iske baad main dobara verify kar lunga jab wo "done" bolein — is dafa exact file names maang lena taake main seedha check kar sakoon.